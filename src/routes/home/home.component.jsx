import Directory from "../../components/directory/directory-component";
import Data from "../../utils/data/data.component";

const Home = () => {
  return <Directory categories={Data} />;
};

export default Home;
